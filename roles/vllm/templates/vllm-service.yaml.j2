apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: {{ llamastack_ns }}
spec:
  selector:
    app: vllm
  ports:
    - name: http
      port: {{ vllm_port }}
      targetPort: {{ vllm_port }}
      protocol: TCP
